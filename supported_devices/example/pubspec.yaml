name: supported_devices_example
description: An example app to showcase supported_devices plugin in action.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  supported_devices:
    path: ..

dev_dependencies:
  flutter_lints: ^1.0.4
